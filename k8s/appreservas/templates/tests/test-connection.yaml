apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appreservas.fullname" . }}-test-connection"
  labels:
    {{- include "appreservas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appreservas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
